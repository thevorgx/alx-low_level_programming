#include "main.h"

/**
 * set_string - Updates the value of a character pointer to a new string.
 * @s: Pointer to a character pointer (two-dimensional input).
 * @to: New string to set for the entry.
 */
void set_string(char **s, char *to)
{
	*s = to;
}
